#{{{ Fixing refresh in linux; need to move mouse to refresh it.
/etc/X11/xorg.conf.d/20-intel.conf
```
Section "Device"
   Identifier  "Intel Graphics"
   Driver      "intel"
   Option      "AccelMethod"  "uxa"
EndSection
```
#}}}
